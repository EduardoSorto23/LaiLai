<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    app:layout_collapseMode="pin"
    android:elevation="4dp"
    android:background="@color/white"
    >

    <TextView
        android:id="@+id/txtPriceCart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Q115"
        android:textAlignment="center"
        android:textSize="25sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnBottomCart"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:fontFamily="@font/neue_kabel" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnBottomCart"
        style="@style/appTheme.MaterialButtonRounded"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:text="@string/view_order"
        android:textColor="@color/white"
        android:fontFamily="@font/neue_kabel_bold"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:backgroundTint="@color/red_app"
        app:strokeColor="@color/red_app"
        app:rippleColor="@color/red_app"
        app:strokeWidth="5dp"
        app:iconPadding="0dp"
        app:iconGravity="textStart"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        />
</androidx.constraintlayout.widget.ConstraintLayout>